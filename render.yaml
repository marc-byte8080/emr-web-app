services:
  - type: web
    name: emr-web-app
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "streamlit run emr_web_app.py --server.port 10000 --server.address 0.0.0.0"
    plan: free
